# Developer tools

このディレクトリには、`grok.com/files` の挙動確認に使った補助スクリプトを置いています。

- `grok-files-probe.js`: DOM 上からメタデータ候補を確認
- `grok-files-network-probe.js`: 通信の観察
- `inspect-first-asset.js`: `rest/assets` の先頭1件を確認

通常利用者は使う必要はありません。
